<?xml version="1.0" ?>
<Page x:Class="The_Untamed_Music_Player.Views.AlbumDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:helper="using:The_Untamed_Music_Player.Helpers"
      xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:local="using:The_Untamed_Music_Player.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:model="using:The_Untamed_Music_Player.Models"
      xmlns:toolkitConverters="using:CommunityToolkit.WinUI.Converters"
      xmlns:ui="using:CommunityToolkit.WinUI"
      Loaded="AlbumDetailsPage_OnLoaded"
      mc:Ignorable="d">

    <Page.Resources>
        <ThemeShadow x:Name="SharedShadow"/>
        <toolkitConverters:EmptyObjectToObjectConverter x:Key="InverseEmptyObjectToVisibilityConverter"
                                                        EmptyValue="Visible" NotEmptyValue="Collapsed"/>
        <DataTemplate x:Key="SongListViewTemplate" x:DataType="model:BriefMusicInfo">
            <Grid Height="52"
                  Background="Transparent" PointerEntered="Grid_PointerEntered"
                  PointerExited="Grid_PointerExited">
                <Grid.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Uid="LocalSongs_Play" Width="216">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE768;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem x:Uid="LocalSongs_PlayNext">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xECC8;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutSubItem x:Uid="LocalSongs_AddTo">
                            <MenuFlyoutSubItem.Icon>
                                <FontIcon Glyph="&#xE710;"/>
                            </MenuFlyoutSubItem.Icon>
                            <MenuFlyoutItem x:Uid="LocalSongs_AddTo_PlayQueue" Width="216">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE90B;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem x:Uid="LocalSongs_AddTo_NewPlaylist">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE710;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyoutSubItem>
                        <MenuFlyoutItem x:Uid="LocalSongs_EditInfo">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE70F;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem x:Uid="LocalSongs_Properties">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE946;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem x:Uid="LocalSongs_ShowArtist">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE77B;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem x:Uid="LocalSongs_Select">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE762;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Grid.ContextFlyout>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="55"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <CheckBox x:Name="ItemCheckBox"
                          Grid.Column="0"
                          IsThreeState="False" Visibility="Collapsed"/>
                <Button x:Name="PlayButton" x:Uid="LocalSongs_PlayButton"
                        Grid.Column="1"
                        Click="PlayButton_Click"
                        DataContext="{x:Bind}"
                        Style="{StaticResource SmallPlayButtonStyle}"
                        Visibility="Collapsed">
                    <FontIcon FontSize="16"
                              Foreground="{ThemeResource AccentTextFillColorTertiaryBrush}"
                              Glyph="&#xE768;"/>
                </Button>
                <TextBlock Grid.Column="2"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{x:Bind GetTextForeground(model:Data.MusicPlayer.CurrentMusic, model:Data.MainViewModel.IsDarkTheme), Mode=OneWay}"
                           Text="{x:Bind Title}"
                           TextTrimming="CharacterEllipsis"
                           ToolTipService.ToolTip="{x:Bind Title}"/>
                <Button Grid.Column="3"
                        Background="Transparent" BorderBrush="Transparent">
                    <TextBlock FontSize="12"
                               Foreground="{x:Bind GetTextForeground(model:Data.MusicPlayer.CurrentMusic, model:Data.MainViewModel.IsDarkTheme), Mode=OneWay}"
                               Text="{x:Bind ArtistsStr}"
                               TextTrimming="CharacterEllipsis"
                               ToolTipService.ToolTip="{x:Bind ArtistsStr}"/>
                </Button>
                <TextBlock Grid.Column="4"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{x:Bind GetTextForeground(model:Data.MusicPlayer.CurrentMusic, model:Data.MainViewModel.IsDarkTheme), Mode=OneWay}"
                           Text="{x:Bind YearStr}"
                           TextTrimming="CharacterEllipsis"
                           ToolTipService.ToolTip="{x:Bind YearStr}"/>
                <TextBlock Grid.Column="5"
                           HorizontalAlignment="Left" VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{x:Bind GetTextForeground(model:Data.MusicPlayer.CurrentMusic, model:Data.MainViewModel.IsDarkTheme), Mode=OneWay}"
                           Text="{x:Bind GenreStr}"
                           TextTrimming="CharacterEllipsis"
                           ToolTipService.ToolTip="{x:Bind GenreStr}"/>
                <TextBlock Grid.Column="6"
                           Margin="0,0,10,0" HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{x:Bind GetTextForeground(model:Data.MusicPlayer.CurrentMusic, model:Data.MainViewModel.IsDarkTheme), Mode=OneWay}"
                           Text="{x:Bind DurationStr}"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Header.Margin" Value="16,54,16,0"/>
                        <Setter Target="TitleText.FontSize" Value="28"/>
                        <Setter Target="SubtitleText.FontSize" Value="14"/>
                        <Setter Target="SongListView.Padding" Value="12,0,12,0"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="850"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Header.Margin" Value="56,54,56,0"/>
                        <Setter Target="TitleText.FontSize" Value="40"/>
                        <Setter Target="SubtitleText.FontSize" Value="20"/>
                        <Setter Target="SongListView.Padding" Value="52,0,52,0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid x:Name="BackgroundHost"
              Grid.Row="0"
              Margin="0,-136,0,0"
              FlowDirection="LeftToRight" SizeChanged="BackgroundHost_OnSizeChanged"/>
        <Grid x:Name="Header"
              Grid.Row="0"
              Margin="0,0,0,-80"
              Canvas.ZIndex="1">
            <Rectangle x:Name="BackgroundAcrylic"
                       Fill="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
                       RadiusX="8" RadiusY="12"
                       Stroke="{ThemeResource ControlElevationBorderBrush}"
                       StrokeThickness="1" Translation="0,0,32"/>

            <Grid x:Name="ContentContainer">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="CoverArt"
                        Grid.Column="0"
                        Width="192" Height="192"
                        Margin="0,0,20,0"
                        BorderBrush="{ThemeResource ControlElevationBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource OverlayCornerRadius}"
                        FlowDirection="LeftToRight"
                        Shadow="{StaticResource SharedShadow}"
                        SizeChanged="AlbumArt_OnSizeChanged" Translation="0,0,16">
                    <Border.Background>
                        <ImageBrush ImageSource="{x:Bind ViewModel.Album.Cover}" Stretch="UniformToFill"/>
                    </Border.Background>
                    <Border Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                            CornerRadius="{x:Bind CoverArt.CornerRadius, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.Album.Cover, Mode=OneWay, Converter={StaticResource InverseEmptyObjectToVisibilityConverter}}">
                        <FontIcon x:Name="CoverArtIcon"
                                  FontSize="64"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Glyph="&#xE93C;"/>
                    </Border>
                </Border>

                <StackPanel x:Name="TextPanel"
                            Grid.Column="1"
                            Orientation="Vertical">
                    <TextBlock x:Name="TitleText"
                               Text="{x:Bind ViewModel.Album.Name}"
                               TextWrapping="NoWrap"/>
                    <TextBlock x:Name="SubtitleText"
                               Margin="0,8,0,0"
                               FontWeight="Normal"
                               Text="{x:Bind ViewModel.Album.GetArtistsStr()}"
                               TextWrapping="NoWrap"/>
                    <TextBlock x:Name="CaptionText"
                               Margin="0,12,0,0"
                               FontSize="14"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Text="{x:Bind ViewModel.Album.GetCountAndDurationStr()}"
                               TextWrapping="NoWrap"/>

                    <Grid x:Name="ButtonPanel">
                        <CommandBar Margin="-4,10,0,0" Padding="0"
                                    HorizontalAlignment="Left"
                                    DefaultLabelPosition="Right">
                            <AppBarButton x:Name="PlayButton"
                                          HorizontalAlignment="Stretch"
                                          BackgroundSizing="OuterBorderEdge"
                                          BorderBrush="{StaticResource AccentButtonBorderBrush}"
                                          XYFocusDown="{x:Bind SongListView}">
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE768;"/>
                                </AppBarButton.Icon>
                            </AppBarButton>

                            <AppBarButton HorizontalAlignment="Stretch"
                                          BorderBrush="{ThemeResource ButtonBorderBrush}"
                                          XYFocusDown="{x:Bind SongListView}"
                                          XYFocusLeft="{x:Bind PlayButton}">
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE8B1;"/>
                                </AppBarButton.Icon>
                            </AppBarButton>
                        </CommandBar>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>

        <ListView x:Name="SongListView"
                  Grid.Row="1"
                  Margin="0,26,0,0"
                  helper:ListViewExtensions.ItemCornerRadius="8"
                  helper:ListViewExtensions.ItemMargin="0,3,0,3"
                  ui:ListViewExtensions.ItemContainerStretchDirection="Horizontal"
                  CanDragItems="True" IsItemClickEnabled="True"
                  ItemClick="{x:Bind ViewModel.SongListView_ItemClick}"
                  ItemTemplate="{StaticResource SongListViewTemplate}"
                  ItemsSource="{x:Bind ViewModel.SongList}"
                  SelectionMode="None"
                  XYFocusUp="{x:Bind PlayButton}">
            <interactivity:Interaction.Behaviors>
                <helper:AlternatingListViewBehavior AlternateBackground="{x:Bind GetAlternateBackgroundBrush(model:Data.MainViewModel.IsDarkTheme), Mode=OneWay}"/>
            </interactivity:Interaction.Behaviors>
        </ListView>
    </Grid>
</Page>
